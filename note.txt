- For commited files only:
git reset --soft id
git reset --mixed
git reset --hard

git log 
git show id
git diff ( for unstaged area)

git restore ( for unstaged area)

- Branching and merging
git checkout -b <branch> (to create and checkout at the same time)
git checkout <branch>
git merge: 
 checkout to A
 merge B
git branch -d <branch> (to delete branch)

- Revert committed files
git revert <commit>

--save: save modules to package.json
